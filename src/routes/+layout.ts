/**
 * Configuration for SvelteKit rendering
 *
 * @property {boolean} csr - Client-side rendering enabled (true)
 * @property {boolean} ssr - Server-side rendering disabled (false)
 *
 * This configuration enables SPA (Single Page Application) mode
 * by enabling client-side rendering and disabling server-side rendering.
 */
export const csr = true;
export const ssr = false;
